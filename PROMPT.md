```
정면에 레이스 코스가 있고, 각 레이서가 점으로 표현돼,
하단에는 버튼이 있어서 레이스를 시작할 수 있고, 나에 해당하는 레이서의 속도를 올리거나, 다른 레이서의 속도를 올릴 수 있어
이걸 웹으로 구현할거야.
```

```
마치 게임 화면 처럼 직선, 곡선이 포함된 평면 코스면 좋겠어
```

```
화면을 원형으로 변경해줘, 두 개의 원형이 있고, 하나는 코스, 하나는 현재 속도등의 정보를 표현할거야
```

```
우측의 버튼들은 시뮬레이션용이야. 하단으로 이동시켜줘
양 원형은 동일한 크기야. 우측화면에는 현재 속도를 표시해줘
```

```
원형은 디스플레이야. 모든 정보가 디스플레이안에 표현되도록 수정해줘
코스는 직선과 곡선을 섞어줘
```

```
코스 길이는 1분30초 정도야. 코스를 좀 더 실제 레이스 코스 처럼 기하학적으로 수정해줘
```

```
코스를 8자 모양으로 변경해줘
```

```
너무 기하학적이야. 좀 더 있을 만한 형태로 변경해줘.
레이스 길이는 입력 받을 수 있게 변경해줘 기본은 1분
```

```
상단 타이틀과 텍스트는 제거하고, 원의 크기를 좀 더 키워줘
레이서는 나, GROC 2025 챔피언, 이번달 1위, 내 친구
```

```
실시간 스피드 패널은 제거해줘. 내 속도 내리기 버튼과 다른 레이서 속도 내리기 버튼도 추가해줘
우측 하단 아나운스 패널을 제어 패널 안으로 이동시켜줘
```

```
버튼 배열을
레이스 시작, 내 속도 올리기, 다른 레이서 속도 올리기
리셋, 내 속도 내리기, 다른 레이서 속도 내리기
이렇게 변겨해줘
```

```
레이스 길이 기본 값 20, 내 아이콘은 좀더 강조되도록
```

```
화면에는 나와 상대방 둘 만 나오게 해줘, 상대방은 GROC 2025 챔피언, 이번 달 1위, 오늘 1위, 내 친구 매번 랜덤이야 
```

```
상대방 아이콘은 빨간색으로 표현해줘
```

```
나와 상대방의 속도는 레이스 시작 시 랜덤하게 변경해줘
```

```
내가 상대방을 제칠 때 좌측 디스플레이가 강조되는 이펙트를 넣어줘,
상대방이 나를 제칠 때, 좌측 디스플레이가 잠깐 흔들리는 이펙트를 넣어줘
```

```
이펙트가 표현이 안되고 있어
```

```
아이콘 기본 크기는 동일하게, 선두의 아이콘을 좀 더 크게,
내가 선두면 좌측 디스플레이 테두리 색이 녹색, 상대가 선두면 빨간색
```

```
아이콘 강조도 선두만 해줘. 테두리 표현이 안되고 있어
```

```
win.mp3 와 lose.mp3 를 추가했어.
내가 선두일 때 win.mp3 를 상대가 선두일 때 lose.mp3 를  출력해줘
```

```
상대가 선두일때도 win.mp3 가 재생되는 것 같아
```

```
위 사진을 기준으로

- 사진의 왼쪽원에 그림이 있는데 이게 웹에서 플레이 하는게 나올 디스플레이야
- 사진의 오른원에 있는 계기판은 일반 계기판 역할을 할거고 레이싱 속도에 따라 변화할거야

위의 조건들을 더해서 차량이 움직이면서
양쪽원에 레이싱을 하면서 디스플레이가 바뀌는 모습이 보이게 해줘
```

```
지금 것 아주 잘했는데
왼쪽 디스플레이에서 서로 앞에가고 뒤에 가는 모습이 잘 안보여
왼쪽 디스플레이 화면 중앙에 2개의 레이싱이 가리지 않게 나오게 해줘
```

```
하단 정보 영역을 좌상단 레이어로, 컨트롤 영역을 우상단 레이어로 만들어줘
```

```
컨트롤 영역 버튼 순서를 조정해줘
Start Race, Reset
Boost Me, Slow Me
Boost Rival, Slow Rival
```

```
컨트롤 영역 가장 아래에 uphill / downhill 토클되는 버튼을 하나 만들거야
downhill 화면은 지금 보이는 화면과 동일하고, uphill 화면은 좌측에는 내 차량만, 우측에는 상대 차량만 나오게 수정해줘
```

```
uphill은 좌측 원형 디스플레이에는 내 차량만, 우측 원형 디스플레이에는 상대 차량만 나오게 수정해줘
```

```
uphill 우측 디스플레이도 좌측 디스플레이와 동일한 배경을 적용해주고, downhill 의 상대 차량과 동일하게 동작시켜줘
```

```
좌측 원형 디스플레이에서 추워 시 차량의 움직임이 부자연스러워,
레이스 종료 구간에 결승점 표현을 추가해줘
```

```
uphill 우측 디스플레이에도 결승점을 표현해줘
상태 표현 테두리를 조금 더 두껍게 수정해줘
```